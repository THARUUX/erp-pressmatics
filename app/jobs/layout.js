import './jobs.css';

export const metadata = {
    title: 'Job Tracker — Pressmatics',
    description: 'Live production job status tracker',
};

export default function JobsLayout({ children }) {
    return <>{children}</>;
}
